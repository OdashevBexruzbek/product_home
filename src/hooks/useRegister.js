import { auth } from "../firebase/firebaseConfig"

import { createUserWithEmailAndPassword } from "firebase/auth"

function useRegister() {
    const registerWithEmailAndPassword = async (displayName)
  return (
    <div></div>
  )
}

export default useRegister